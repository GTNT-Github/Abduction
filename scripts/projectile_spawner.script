function init(self)
	timer.delay(math.random(0.5,1), false, spawn)
	
end

function spawn()
	
	factory.create("#projectiles", vmath.vector3(0,0,0))
	local timer = timer.delay(math.random(1,2), false, spawn)
end
